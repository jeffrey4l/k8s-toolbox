apiVersion: v1
kind: Service
metadata:
  name: {{ include "opstool.fullname" . }}
  labels:
    {{- include "opstool.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opstool.selectorLabels" . | nindent 4 }}
