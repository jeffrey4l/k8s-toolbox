apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opstool.fullname" . }}-test-connection"
  labels:
    {{- include "opstool.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opstool.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
